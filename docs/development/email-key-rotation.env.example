# 서비스 중지 후 키 교체 명령에만 주입할 환경변수 예시.
# 실제 값은 비밀 설정에 저장하세요. 명령 인자·Git·로그에는 넣지 마세요.
DB_HOST=
DB_PORT=5432
DB_NAME=
DB_USER=
DB_PASSWORD=

# 현재 DB를 암호화한 키. 재시작 시 사용하던 값을 유지합니다.
EMAIL_ENCRYPTION_KEY=
# 새 32바이트 난수의 Base64 문자열. apply에만 필요합니다.
EMAIL_ENCRYPTION_NEXT_KEY=
